<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.goldendust.profile.dao.BoardDao">
	
	<insert id="insert">
		INSERT INTO board(pnum, mid, mname, ptitle, pcontent, viewcount, numcomments) 
			VALUES (SEQ_PNUM.nextval, #{param1}, #{param2}, #{param3}, #{param4}, 0, 0)
	</insert>

	<select id="getList" resultType="com.goldendust.profile.dto.BoardDto">
		SELECT * FROM 
			(SELECT A.*, CEIL((ROWNUM / #{param2}))pagenum, ROWNUM
				FROM
					(SELECT * FROM board ORDER BY PNUM DESC)A) <!-- 정렬결과를 A로 치환 -->
						WHERE pagenum=#{param1}
	</select>
	
	<select id="findByPnum" resultType="com.goldendust.profile.dto.BoardDto">
		SELECT * FROM board WHERE PNUM=#{param1}
	</select>
	
	<update id="modifyPost">
		UPDATE board SET ptitle=#{param2}, pcontent=#{param3} WHERE pnum=#{param1} 
	</update>
	
	<delete id="deletePost">
		DELETE FROM board WHERE PNUM=#{param1}
	</delete>
	
	<select id="getTotalPostCount" resultType="int">
		SELECT COUNT(*) FROM board
	</select>
	
	<select id="getSearchResultTotalCount" resultType="int">
		SELECT COUNT(*) FROM board WHERE ptitle LIKE '%' || #{param1}|| '%' OR pcontent LIKE '%' || #{param1} || '%' ORDER BY PNUM DESC
	</select>
	
	<select id="getSearchResult" resultType="com.goldendust.profile.dto.BoardDto">
		SELECT * FROM 
			(SELECT A.*, CEIL((ROWNUM / #{param2}))pagenum, ROWNUM
				FROM
					(SELECT * FROM board WHERE ptitle LIKE '%' || #{param3}|| '%' OR pcontent LIKE '%' || #{param3} || '%' ORDER BY PNUM DESC)A) <!-- 정렬결과를 A로 치환 -->
						WHERE pagenum=#{param1}
	</select>
	
	<select id="findMidByPnum" resultType="String">
		SELECT mid FROM board WHERE PNUM=#{param1}
	</select>
	
	<update id="setCommentCount">
		UPDATE board SET numcomments=#{param2} WHERE pnum=#{param1}
	</update>
	
	<update id="incrementViewCount">
		UPDATE board SET viewcount=viewcount+1 WHERE pnum=#{param1}
	</update>
	
	<!-- temp -->
	<update id="resetViewCount">
		UPDATE board SET viewcount=0 WHERE pnum=#{param1}
	</update>
	<update id="resetAllViewCount">
		UPDATE board SET viewcount=0
	</update>
	
</mapper>